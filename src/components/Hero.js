import React, { Fragment } from 'react';
import logo from '../img/logo.svg';

const Hero = () => {
    return (
        <div>
            <h1>Hero</h1>
            <img src={logo}alt="logo" />
        </div>
    );
}
 
export default Hero;